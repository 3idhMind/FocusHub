import { getCurrentUser } from "../../core/auth.js";
import { db } from "../../core/db.js";
import { doc, getDoc, setDoc } from "firebase/firestore";

export function init() {
    console.log("Syllabus Module: Coming Soon mode.");
}

window.syllabusModule = {};
